<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LIVREca" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Definire culori utilizate în tema aplicației -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/brown</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/brown</item>
        <item name="colorOnSecondary">@color/beige</item>
        <item name="colorOnBackground">@color/black</item>
        <item name="colorSurface">@color/beige</item>
        <item name="colorOnSurface">@color/brown</item>
    </style>

    <!-- Tema aplicației, derivată din tema de bază -->
    <style name="Theme.LIVREca" parent="Base.Theme.LIVREca">
        <!-- Poți adăuga aici alte stiluri specifice pentru tema aplicației tale -->
    </style>

    <!--  Light mode -->
    <!-- Screen background if light mode on -->
    <style name="SCREEN">
        <item name="android:background">@drawable/gradient_app_background</item>
    </style>

    <!-- Text background if light mode on -->
    <style name="TEXTVIEW">
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="EDITTEXT">
        <item name="android:textColor">@drawable/text_selector_color</item>
    </style>

    <!-- Button background if light mode on -->
    <style name="BUTTON">
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">@color/white</item>
    </style>


</resources>
